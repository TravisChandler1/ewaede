{
  "name": "ewaede-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Ewa Ede - Yoruba Learning Platform",
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run vercel-build",
    "vercel-build": "cd apps/web && npm run vercel-build",
    "deploy": "cd apps/web && npm run deploy"
  },
  "workspaces": [
    "apps/web"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}